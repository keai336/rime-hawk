# aux_v2_debug.lua 完整知识库

> 本文档汇总了关于 Rime 辅码插件 `aux_v2_debug.lua` 的全部已知信息，供专业写手据此撰写最终 README。

---

## 一、插件定位

- **它是什么**：一个 Rime Lua **候选过滤器**(filter)，不是 translator。它在候选列表的**后处理阶段**工作，不改变引擎的编码解析行为。
- **解决什么问题**：拼音输入法同音字太多，候选列表太长。本插件在拼音（音维度）之后追加一个**辅码**（形维度），实时筛选候选。
- **核心公式**：`拼音 + 辅码 = 精准定位`

---

## 二、部署配置

### 2.1 文件放置

```
<Rime用户目录>/lua/
├── aux_v2_debug.lua    ← 脚本本体
└── test5.txt           ← 辅码表文件
```

### 2.2 方案 YAML 配置

```yaml
engine:
  filters:
    - lua_filter@aux_v2_debug@test5@on@;@`@,@s
```

参数格式（`@` 分隔，**每项必填**）：

| # | 参数 | 说明 | 示例 |
|---|------|------|------|
| 1 | path | 辅码表文件名（不带 .txt） | `test5` |
| 2 | showor | 是否显示辅码提示 | `on` / `off` |
| 3 | trigger | 引导键 | `;` |
| 4 | switch | 单字/词模式切换键 | `` ` `` |
| 5 | ph | 占位符（主动分心用） | `,` |
| 6 | matchmode | 匹配模式 | `s`(简码) / `m`(全码) |

### 2.3 关键注意事项

1. **preedit_format 冲突**：如果方案中有 `translator/preedit_format`，必须改名为 `preedit_format1`，否则脚本音节切分会失败。
2. **speller/alphabet**：必须添加引导键和切换键：`'zyxwvutsrqponmlkjihgfedcba;`,'`
3. 重新部署 Rime 后生效。

---

## 三、辅码表格式

纯文本文件（UTF-8），每行格式为：

```
字<TAB>辅码
```

- 辅码通常两个字母
- 一个字可以有多个辅码（逗号分隔）
- 同一字可出现多行（会被合并）
- 本文档所有示例使用 `test5.txt`（虎码拆分首末大码）

### 示例辅码数据（已验证）

| 字 | 辅码 | 验证来源 |
|----|------|----------|
| 心 | hh | 截图验证 |
| 仪 | jb | 截图验证 |
| 观 | re | 截图验证 |
| 一 | fi | 截图验证 |
| 踩 | pe | 旧文档 |
| 啊 | dz | test5.txt |
| 阿 | tz | test5.txt |
| 爱 | yr | test5.txt |
| 最 | or | 截图验证 |
| 嘴 | dt | 截图验证 |
| 祎 | fg | 截图验证 |

---

## 四、功能详解与真实实例

> ⚠️ 以下所有实例均来自真实截图，候选顺序和内容均经过验证。

### 4.1 辅码筛选

**核心功能**。输入拼音后，按引导键 `;` + 辅码字母，对候选进行实时筛选。

#### 单码筛选

输入辅码第一位，保留辅码首位匹配的候选。

#### 双码精确筛选

输入两位辅码，首位和末位都匹配才保留。一般 4 键（双拼2+辅码2）定位一个字。

#### 辅码提示

当 `showor=on` 时，候选旁显示辅码：`踩(pe) 菜(le) 才(nt)`

#### 匹配模式

- **简码模式(s)**：单码只匹配辅码首位
- **全码模式(m)**：单码同时匹配首位和末位，双码顺序可互换

### 4.2 偏移指令

在两位辅码之后输入偏移键，调整候选截取长度。

| 按键 | 方向 | 偏移量 | 效果 |
|------|------|--------|------|
| `a` | 左偏 | 1 | 候选长度 -1 |
| `s` | 左偏 | 2 | 候选长度 -2 |
| `d` | 右偏 | 1 | 候选长度 +1 |
| `f` | 右偏 | 2 | 候选长度 +2 |

> ⚠️ 偏移码只能在两个辅码之后使用。

#### 候选跳过 `w`

`w` 键跳过当前第一个候选，让第二个升为首位。

### 4.3 分心机制

独创的「跨字分配辅码」。打二字词时，两位辅码分别作用于词中的不同字。

#### 被动分心 `**`（双码命中，真实实例）

**场景**：想打「心仪」（心=hh, 仪=jb）

```
步骤 1：输入 xinyi
  候选框：① 心怡  ② 新一  ③ 心意  ④ 新意  ⑤ 心仪
  → 心仪排在第 ⑤ 位

步骤 2：键入 ;h
  候选框：① 心意  ② 心仪  ③ 欣怡  ④ 馨怡  ⑤ 心疑
  → 单码 h 筛选，心仪升到第 ② 位

步骤 3：继续输入 j（组合成 hj）
  候选框：① 心仪 **
  → 双码 hj 找不到辅码恰好为 hj 的单字
  → 被动分心触发：h → 心(hh) ✓，j → 仪(jb) ✓
  → 「心仪」整词以 ** 标记展示
```

**`**` 的条件**：两位辅码分别匹配二字词的第一个字和第二个字的辅码首位。

#### 被动分心 `*x`（首码命中+次码延续，真实实例）

**场景**：同样输入 xinyi，但辅码为 hf

```
步骤 1-2 同上

步骤 3：输入 hf
  候选框：① 心 *x
  → h → 心(hh) ✓
  → f → 仪(jb)? ✗  意? ✗  怡? ✗
  → 第二位辅码 f 匹配不到任何词的次字
  → 候选截断为单字「心」，标注 *x
  → 辅码 f 被保留为"遗留辅码"

步骤 4：确认心后，遗留辅码 f 自动作用
  候选框：① 一 (fi)  ② 口  ③ 祎 (fg)
  → 辅码 f 筛选音节 yi 下的候选
```

**`**` vs `*x` 对比**：

| 标记 | 含义 | 候选展示 | 辅码去向 |
|------|------|----------|----------|
| `**` | 两位都命中 | 整词 | 消耗完毕 |
| `*x` | 仅首码命中 | 截断为首字 | 次码保留给下一轮 |

#### 主动分心 `✂`

在辅码后追加占位符 `,`，强制触发跨字分配。标注 `✂` 和 `⇐`。

```
输入：xinyi;hj,
候选：① 心仪 ✂
```

### 4.4 单字筛选模式（真实实例）

**切换方式**：键入引导键 `;` 后紧跟切换键 `` ` ``

**场景**：想逐字打出「单字模式」

```
步骤 1：输入 danzimoshi
  候选框：① 单子模式  ② 单字  ③ 单子  ④ 胆子  ⑤ 担子
  → 正常词模式

步骤 2：键入 ;`
  → 切换到单字筛选模式

步骤 3：候选变为纯单字
  候选框：① 单(hn)  ② 但(jf)  ③ 旦(of)  ④ 淡(kc)  ⑤ 蛋(tc)
  → 只显示第一个音节匹配的单字

步骤 4：确认「单」后进入下一音节
  候选框：① 子(hi)  ② 字(wh)  ③ 自(oi)  ④ 紫(hn)  ⑤ 姊(hy)

步骤 5：使用 w 跳过「子」
  候选框：① 字(wh)  ② 自(oi)  ③ 紫(hn)  ④ 姊(hy)
  → 「字」升为首位

步骤 6：以此类推完成剩余字
```

- 当所有字选完 commit 时，自动切换回词模式
- 单字模式下也可以使用辅码筛选

### 4.5 长句断句（真实实例）

**场景**：想打「我的无力成了我的败笔」

```
步骤 1：输入所有音
  候选框：① 我的物理成了我的败笔
  → 引擎把「无力」识别成了「物理」
  → 想要的「无」辅码是 dd

步骤 2：键入 ;d（单码）
  候选框：① 我的<...
  → 辅码 d 在多个音节上匹配，标记断点

步骤 3：继续输入 d（组合成 dd）
  候选框：① 我的<无力成了我的败笔
  → dd 匹配到 wu(无) 和 li(力) 两个断点
  → 用 < 标记断点位置

步骤 4：按 ;（执行断句）
  候选框：① 我的无
  → 在第一个断点 <无 处截断

步骤 5：确认后，辅码 dd 继续作用！
  候选框：① 力成了我的败笔
  → dd 继续筛选剩余候选
```

**关键特性：辅码持续作用** — 断句确认后辅码不会被清除，继续对后续候选生效。

#### 断句原理

脚本读取首候选的 `preedit`（音节序列），逐个音节通过 Memory API 查询该音节下所有字的辅码集合，找到匹配的音节位置作为断点。

#### 断句中的偏移

```
……;dd;a  → 在断点基础上左偏 1（少截一个字）
……;dd;d  → 在断点基础上右偏 1（多截一个字）
```

#### 多次断句

```
……;dd;     → 在第一个断点处截断
……;dd;;    → 跳到第二个断点
……;dd;;;   → 跳到第三个断点
```

### 4.6 修音（真实实例）

断句的延伸功能。在第二个 `;` 之后输入 `s` + 正确音节。

**场景**：想打「最佳歌手」，但 gr 被解析为 guan（观）

```
步骤 1：输入 zvjxgruz
  候选框：① 最佳观收  ② 最佳  ③ 醉驾  ④ 最(or)  ⑤ 嘴(dt)
  → gr 被解析为 guan（观），想要的是 ge（歌）

步骤 2：键入 ;r（单码）
  候选框：① 最<佳<观收
  → 辅码 r 标记出两个潜在断点（佳、观）

步骤 3：继续输入 e（组合成 re）
  候选框：① 最佳<观收
  → 双码 re 精确匹配到「观」(辅码 re) → 断点定位到观

步骤 4：按 ; 执行断句
  候选框：① 最佳

步骤 5：输入修音指令 sge
  候选框：① gr->ge
  → 脚本定位到断点所在的音节 gr（guan/观）
  → 将其替换为 ge（歌）

步骤 6：确认后整句编码被重写
  候选框：① 最佳歌手  ② 最佳  ③ 醉驾  ④ 最(or)  ⑤ 嘴(dt)
  → 音节从 zui jia guan shou → zui jia ge shou
```

**修音本质**：不是改字，是改**音节编码**，让引擎重新解析。双拼场景特别有用——两个键可能被引擎拆分到相邻音节。

### 4.7 动态性

对第一候选的特殊动态优化。

**逻辑**：你输入第一个辅码时给你展示了第一候选，你继续输入第二个辅码说明不满意，就过滤掉。

**设计哲学**：辅码阶段，键键有反馈。基于此甚至可以关掉候选框盲打。

### 4.8 智能指令

#### 复制控制（c 系列）

| 序列 | 效果 |
|------|------|
| `c` | 进入复制模式（1次） |
| `cc` | +1（=2次） |
| `cv` | ×2 翻倍 |
| `cb` | 平方 |
| `cn` | -1 |

#### 翻译控制

| 序列 | 效果 |
|------|------|
| `t` | 简繁转换（OpenCC s2t） |
| `tXX` | 翻译到目标XX（需本地HTTP服务） |
| `rXX` | 原始输入码请求翻译接口XX |

---

## 五、输入模式总览

| 输入形式 | 处理分支 |
|----------|----------|
| `pinyin` | 直通（默认） |
| `pinyin;aux` | 普通辅码筛选 |
| `pinyin;aux`` ` | 单字模式切换 |
| `pinyin;aux;` | 长句断句 |
| `pinyin;aux;sfoo` | 修音 |

**功能联动关系**：
- 辅码 + 偏移 = 精确定位单字
- 辅码 + 分心 = 跨字分配
- 断句 + 偏移 = 灵活控制断点
- 断句 + 修音 = 纠正错误音节
- 动态性贯穿所有模式

---

## 六、运行环境与依赖

本脚本**不能独立运行**，依赖 Rime Lua 运行环境。

### 必要依赖

| 组件 | 用途 |
|------|------|
| `rime_api` | 获取用户数据目录 |
| `Memory` | 查询词典，支撑断句功能 |
| `Projection` | 处理 preedit_format 转换 |
| `Opencc` | 简繁转换 |
| `Candidate` | 构造自定义候选 |

### 可选依赖

| 组件 | 用途 | 缺失时行为 |
|------|------|-----------|
| `simplehttp` | HTTP请求 | 回退空实现，翻译不可用 |
| 本地HTTP服务 127.0.0.1:8080 | 翻译接口 | 翻译返回空 |

---

## 七、日志系统

### 结构化调试日志

- **开关**：`DEBUG_MODE = true/false`
- **文件**：`<Rime用户目录>/debug_breakpoint.log`
- **格式**：JSON Lines
- **内容**：断句匹配、Memory查询、分支分流
- **解析工具**：`docs/log_parser.html`

### 普通运行日志

- **文件**：`<Rime用户目录>/dic.log`
- **内容**：初始化、辅码表加载、Memory初始化

---

## 八、版本历史

### v2 (推荐) — 修复修音崩溃

- **问题**：修音分支直接修改 `inputspls` 数组，污染 `split_pinyin()` 缓存，导致越界崩溃
- **修复**：创建副本 `fresh_spls`，不直接修改原数组；增加越界安全检查

### v1 — 核心重构

- 用 Memory API 替代音码列（`comb_code[音码]`）
- 删除 `comb_code`、`zi_to_yin`、`comb_expanded` 等
- 内存节省约 60%

---

## 九、已知问题与限制

1. **简拼断句可能失败**：`dict_lookup` 对简拼音节可能返回空
2. **翻译功能依赖本地HTTP服务**：纯离线只有 OpenCC 简繁可用
3. **辅码表编码**：必须 UTF-8，制表符分隔
4. **preedit_format 冲突**：必须改名为 preedit_format1

---

## 十、文件结构

```
refactor/
├── aux_v2_debug.lua      # 核心脚本（部署唯一必需的 Lua 文件）
├── test5.txt             # 辅码表示例（虎码首末大码）
├── CHANGELOG.md          # 版本历史
├── README.md             # 用户文档
└── docs/
    ├── api_reference.md
    ├── data_flow.md
    └── technical_docs.md
```
